all:
  children:
    new:
      hosts:
      
        #piserve:
          # ansible_host: piserve.local
          # ansible_host: fe80::bdae:5ca9:9f04:5e7%bridge100
          # ansible_host: 192.168.0.30
          # ansible_host: piservw
          # ansible_user: nico
        
        pi1:
          # ansible_host: 192.168.2.2 # Ip address of the Pi or hostname
          ansible_host: "pi1.local"
          force_hostname: "pi1"
          ansible_user: pi
          raspberry_pi_gpu_mem: '16'
          ansible_python_interpreter: /usr/bin/python3
          headless: true


    raspberrypi:
      hosts:
        pi5:
          # ansible_host: 192.168.0.30
          ansible_host: "piserv.local"
          force_hostname: "piserve"
          ansible_user: pi
          raspberry_pi_gpu_mem: '512'
          ansible_python_interpreter: /usr/bin/python3
          external_drive: /dev/sda1
          # raspberry_pi_sda_spindown : 120 # 2 minutes. Modify here if you want to change the spindown time for this host only.
        pidac:
          ansible_host: 192.168.0.23
          ansible_python_interpreter: /usr/bin/python3
          dac_usage: true
          # external_drive: /dev/sda1
          # raspberry_pi_sda_spindown : 120 # 2 minutes. Modify here if you want to change the spindown time for this host only.
        pi4:
          ansible_host: "pi4w.local"
          force_hostname: "pi4w"
          ansible_user: pi
          raspberry_pi_gpu_mem: '512'
          ansible_python_interpreter: /usr/bin/python3
          # external_drive: /dev/sda1
          # raspberry_pi_sda_spindown : 120 # 2 minutes. Modify here if you want to change the spindown time for this host only.